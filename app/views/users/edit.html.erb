<h1>Tell Us What You Are Looking For!</h1>

		<%= form_for current_user do |f| %>
		<%= f.label :address %> <br />
		<%= f.text_field :address %><br />
		<%= f.label :address_title %> <br />
		<%= f.text_field :address_title %> <br />
		<%= f.submit "View Map"%>
		<%end%>
 
		<%= collection_select(:address, :user_id, User.all, :id, :address, prompt: true) %>


	<%= link_to 'Manage Addresses', user_addresses_path(current_user, @address), method: :get, :style => "color:black" %>

	 <%= link_to 'Sign out', destroy_user_session_path, method: :delete, :style => "color:black"  %>